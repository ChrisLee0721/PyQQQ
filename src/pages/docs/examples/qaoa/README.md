# QAOA 求解 MaxCut

求解三角形图（3 顶点、3 条边）的最大割，答案 = 2。需要 scipy。

MaxCut on a triangle graph (3 vertices, 3 edges); answer = 2. Requires scipy.

## 运行 Run

```bash
python examples/qaoa/qaoa.py
```

## 预期输出 Expected output

约 2.0。

Around 2.0.
