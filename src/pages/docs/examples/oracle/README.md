# @oracle decorator @oracle 装饰器

把经典谓词 x == 5 编译成 Grover 相位神谕，搜索放大 |101> 态。

Compile the classical predicate x == 5 into a Grover phase oracle; the search amplifies |101>.

## 运行 Run

```bash
python examples/oracle/oracle.py
```

## 预期输出 Expected output

计数直方图几乎全部集中在 |101>。

The counts are almost entirely on |101>.
