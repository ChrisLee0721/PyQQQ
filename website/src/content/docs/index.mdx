---
title: QuoNic
description: Quantum programming, as simple as writing Python
template: splash
hero:
  title: |
    QuoNic
    <span style="font-size: 0.5em; opacity: 0.8;">Quantum Programming, as Simple as Writing Python</span>
  tagline: 19 backends · 75 algorithms · 771 tests · 3 real hardware platforms
  actions:
    - text: Quick Start
      link: /start/quickstart
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/ChrisLee0721/QuoNic
      icon: external
      variant: secondary
---

import { Card, CardGrid, Tabs, TabItem, Code } from '@astrojs/starlight/components';

## Why QuoNic?

<CardGrid>
  <Card title="3 Lines, Not 10" icon="rocket">
    Bell state in 3 lines. Same circuit takes 10+ lines in raw Qiskit.
  </Card>
  <Card title="19 Backends" icon="server">
    IBM · Google · AWS · Azure · IonQ · Rigetti · QuEra · Xanadu · Quantum Inspire · Origin Quantum · Qulacs · TensorCircuit · CUDA-Q · MindQuantum · QPanda3 · CqLib · PennyLane · Native · CuPy
  </Card>
  <Card title="75 Algorithms" icon="cpu">
    Grover · VQE · QAOA · QFT · QPE · Shor · Teleportation · BB84 · QEC · QML · and 65 more
  </Card>
  <Card title="3 Real Hardware" icon="zap">
    ✅ Origin Quantum WK_C180 · ✅ AWS Rigetti Cepheus · ✅ Quantum Inspire Tuna
  </Card>
</CardGrid>

## Code Comparison

<Tabs>
  <TabItem label="QuoNic (3 lines)">
    ```python
    from quonic import qgate, qshow
    from quonic.gates import H, CX
    qgate(H, 0)
    qgate(CX, 0, 1)
    qshow()
    ```
  </TabItem>
  <TabItem label="Qiskit (10 lines)">
    ```python
    from qiskit import QuantumCircuit
    from qiskit_aer import AerSimulator

    qc = QuantumCircuit(2, 2)
    qc.h(0)
    qc.cx(0, 1)
    qc.measure([0, 1], [0, 1])

    simulator = AerSimulator()
    result = simulator.run(qc, shots=1024).result()
    print(result.get_counts())
    ```
  </TabItem>
  <TabItem label="Cirq (8 lines)">
    ```python
    import cirq

    q0, q1 = cirq.LineQubit.range(2)
    circuit = cirq.Circuit([
        cirq.H(q0),
        cirq.CNOT(q0, q1),
        cirq.measure(q0, q1)
    ])
    result = cirq.Simulator().run(circuit, repetitions=1024)
    ```
  </TabItem>
</Tabs>

## Key Features

### Smart Scheduling
```python
qshow()  # Automatically picks the best backend
```

### Error Mitigation
```python
from quonic import zne
result = zne(circuit, noise=0.05, extrapolation="linear")
```

### Quantum Error Correction
```python
from quonic.qec import BitFlipCode, qec_round_trip
result = qec_round_trip(code="bit_flip", error_rate=0.01)
```

### Pulse-Level Control
```python
from quonic.pulse import grape_optimize
result = grape_optimize(target_unitary, n_steps=50)
```

### ZX-Calculus Optimization
```python
from quonic.zx import circuit_to_zx, optimize_zx, extract_circuit
graph = circuit_to_zx(circuit)
simplified = optimize_zx(graph)
optimized = extract_circuit(simplified)
```

## Real Hardware Verified

| Platform | Device | Status |
|----------|--------|--------|
| **Origin Quantum** | WK_C180 | ✅ Verified |
| **AWS Braket** | Rigetti Cepheus-1-108Q | ✅ Verified |
| **Quantum Inspire** | Tuna-9 / Tuna-17 | ✅ Verified |

## Install

```bash
pip install quonic
```

## Stats

- **771** tests passing
- **19** backends
- **75** algorithm templates
- **106** examples (bilingual)
- **5** tutorial notebooks
- **3** real hardware platforms
