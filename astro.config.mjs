// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
	site: 'https://chrislee0721.github.io/QuoNic/',
	// Serve static HTML files from src/pages/ as-is
	output: 'static',
});
